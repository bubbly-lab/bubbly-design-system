{
  "name": "@bubbly-design-system/project",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "check-types": "turbo run check-types",
    "storybook:react": "pnpm --filter @bubbly-design-system/react storybook",
    "storybook:icons": "pnpm --filter @bubbly-design-system/icons storybook",
    "changeset": "changeset",
    "ci:publish": "cd packages/icons && npm publish --provenance --access public"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.22.0",
  "devDependencies": {
    "@biomejs/biome": "^2.2.5",
    "@bubbly-lab/biome-config": "^1.0.0",
    "@changesets/cli": "^2.30.0",
    "turbo": "^2.5.8",
    "typescript": "^5.9.3"
  }
}
